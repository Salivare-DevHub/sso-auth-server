# SSO Auth Server

Minimal SSO auth service for OAuth-based login and token issuance.

## Contract
This service uses the protobuf contract from `github.com/salivare-io/protos-sso`.

## OpenAPI
The generated OpenAPI spec is available at `gen/openapi/sso/sso_auth.swagger.json`.
